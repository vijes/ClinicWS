services:
  - type: web
    name: clinica-backend
    runtime: java
    plan: free
    buildCommand: "./gradlew build -x test"
    startCommand: "java -jar build/libs/*.jar"
    envVars:
      - key: JAVA_VERSION
        value: 21